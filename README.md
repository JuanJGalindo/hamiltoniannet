# HamiltonianNet (hnet)

A modular framework for Hamiltonian Neural Networks — from canonical conservative systems to symplectic neural operators.

```bash
pip install hamiltoniannet
```

See `docs/` and `examples/` for tutorials.
