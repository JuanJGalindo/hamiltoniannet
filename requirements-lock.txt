# hamiltoniannet — pinned lock of the exact versions validated on 2026-06-29
# (Windows, Python 3.14, CPU PyTorch build). Covers core + dev tooling.
# Curated to direct dependencies; for a full environment snapshot run:
#   pip freeze > requirements-freeze.txt
#
# PyTorch is the CPU wheel — install it first from its index:
#   pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-lock.txt

# ── core ──
torch==2.13.0
numpy==2.4.4
scipy==1.18.0
matplotlib==3.11.0

# ── dev tooling ──
pytest==9.1.1
pytest-cov==7.1.0
coverage==7.14.3
import-linter==2.12
ruff==0.15.20
mypy==2.1.0
pre-commit==4.6.0
marimo==0.23.11
